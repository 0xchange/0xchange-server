var express = require('express');

var router = express.Router();

/*
router.get('/get', _);

router.get('/get/:token', _);

router.post('/post/order/:order', _);

router.post('/post/token/:token', _);
*/

module.exports = router;
